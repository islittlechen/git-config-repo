server:
  port: 1110

eureka:
  instance:
    hostname: localhost
    data-center-info:
      name: mydatacenter
    environment:
      name: dev
  client:
    registerWithEureka: false
    fetchRegistry: false
    defaultZone: http://localhost:1111/eureka/,http://localhost:1112/eureka/

management:
  security:
    enabled: false

security:
  basic:
    enabled: true
  user:
    name: root
    password: 123456