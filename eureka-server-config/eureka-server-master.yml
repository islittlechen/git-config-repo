server:
  port: 1110

eureka:
  instance:
    hostname: localhost
    data-center-info:
      name: mydatacenter
    environment:
      name: dev
  client:
    register-with-eureka: false
    fetch-registry: false

security:
  basic:
    enabled: true
  user:
    name: root
    password: 123456