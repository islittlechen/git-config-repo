server:
  port: 1112

eureka:
  instance:
    hostname: localhost
    data-center-info:
      name: mydatacenter
    environment:
      name: dev
  client:
    defaultZone: http://localhost:1110/eureka/,http://localhost:1111/eureka/

management:
  security:
    enabled: false

security:
  basic:
    enabled: true
  user:
    name: root
    password: 123456