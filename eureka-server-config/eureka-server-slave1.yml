server:
  port: 1111

eureka:
  instance:
    hostname: peer2
    data-center-info:
      name: mydatacenter
    environment:
      name: dev
  client:
    service-url:
      defaultZone: http://peer1:1110/eureka/,http://peer3:1112/eureka/

management:
  security:
    enabled: false

security:
  basic:
    enabled: true
  user:
    name: root
    password: 123456